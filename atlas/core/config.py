"""Atlas configuration — Pydantic settings for all config.

TODO: Implement after scaffolding is approved.
- AtlasSettings class loading from .env
- Database, Redis, Telegram, API settings
- Quiet hours, digest interval, timezone
- Feature flags
- Secrets abstraction (never expose raw values in logs)
"""
