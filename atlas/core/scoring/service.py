"""Scoring service — compute composite scores for signals and opportunities.

TODO: Implement after scaffolding is approved.
- score_signal(signal, weights) -> float
- score_opportunity(opportunity, weights) -> float
- rank_items(items, scorer, limit) -> list
- get_weight_profile(domain) -> WeightProfile
- update_weight_profile(domain, weights) -> None
"""
