"""Atlas integration adapters — external service connectors.

Each adapter provides a clean interface to an external service.
All adapters are stubs until credentials are configured.
No adapter should ever fabricate a working connection without real credentials.
"""
